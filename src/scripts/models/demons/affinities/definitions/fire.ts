import { DemonAffinityDefinition } from '../affinity'

const fire: DemonAffinityDefinition = {
    id: 'fire',
    name: 'Fire',
    baseStats: {
        strength: 10,
        magic: 10,
        armor: 10,
        speed: 10
    }
}

export { fire }